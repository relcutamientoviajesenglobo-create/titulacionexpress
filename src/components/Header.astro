---
interface Props {
  currentPath?: string;
}
const { currentPath = '' } = Astro.props;

const carrerasReguladas = [
  { nombre: 'Derecho', slug: 'derecho' },
  { nombre: 'Contaduría', slug: 'contaduria' },
  { nombre: 'Ingeniería Industrial', slug: 'ingenieria-industrial' },
  { nombre: 'Ingeniería Computacional', slug: 'ingenieria-computacional' },
];

const carrerasNoReguladas = [
  { nombre: 'Administración', slug: 'administracion' },
  { nombre: 'Mercadotecnia', slug: 'mercadotecnia' },
  { nombre: 'Pedagogía', slug: 'pedagogia' },
  { nombre: 'Ciencias de la Comunicación', slug: 'ciencias-de-la-comunicacion' },
  { nombre: 'Ciencias Políticas', slug: 'ciencias-politicas' },
  { nombre: 'Turismo', slug: 'turismo' },
  { nombre: 'Gastronomía', slug: 'gastronomia' },
  { nombre: 'Diseño Gráfico', slug: 'diseno-grafico' },
  { nombre: 'Economía', slug: 'economia' },
  { nombre: 'Comercio Internacional', slug: 'comercio-internacional' },
  { nombre: 'Educación Preescolar', slug: 'educacion-preescolar' },
];

const recursos = [
  { nombre: 'Acuerdo 286 — Guía Completa', url: '/acuerdo-286/' },
  { nombre: 'Requisitos', url: '/titulacion-por-experiencia-laboral/requisitos/' },
  { nombre: 'Proceso Paso a Paso', url: '/titulacion-por-experiencia-laboral/proceso-paso-a-paso/' },
  { nombre: 'Costos', url: '/titulacion-por-experiencia-laboral/costo/' },
  { nombre: 'Cédula Profesional', url: '/cedula-profesional/' },
  { nombre: 'Preguntas Frecuentes', url: '/titulacion-por-experiencia-laboral/preguntas-frecuentes/' },
];
---

<header class="header" id="header">
  <div class="header__inner container">
    <a href="/" class="header__logo" aria-label="Titulación Express — Inicio">
      <span class="header__logo-icon">🎓</span>
      <span class="header__logo-text"><strong>Titulación</strong> Express</span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="header__nav" aria-label="Navegación principal">
      <ul class="header__menu">
        <li class="header__item">
          <a href="/titulacion-por-experiencia-laboral/" class="header__link">Titulación</a>
        </li>
        <li class="header__item header__item--mega">
          <button class="header__link header__link--dropdown" aria-expanded="false" aria-haspopup="true">
            Carreras <span class="header__chevron" aria-hidden="true">▾</span>
          </button>
          <div class="mega-menu" role="menu" aria-label="Carreras disponibles">
            <div class="mega-menu__inner container">
              <div class="mega-menu__col">
                <h3 class="mega-menu__heading">Carreras Reguladas</h3>
                <p class="mega-menu__subtext">Requieren 50% de créditos universitarios</p>
                <ul class="mega-menu__list">
                  {carrerasReguladas.map(c => (
                    <li><a href={`/carreras/${c.slug}/`} class="mega-menu__link">{c.nombre}</a></li>
                  ))}
                </ul>
              </div>
              <div class="mega-menu__col">
                <h3 class="mega-menu__heading">Carreras No Reguladas</h3>
                <p class="mega-menu__subtext">Sin requisito de créditos previos</p>
                <ul class="mega-menu__list">
                  {carrerasNoReguladas.map(c => (
                    <li><a href={`/carreras/${c.slug}/`} class="mega-menu__link">{c.nombre}</a></li>
                  ))}
                </ul>
              </div>
              <div class="mega-menu__col mega-menu__col--recursos">
                <h3 class="mega-menu__heading">Recursos</h3>
                <ul class="mega-menu__list">
                  {recursos.map(r => (
                    <li><a href={r.url} class="mega-menu__link">{r.nombre}</a></li>
                  ))}
                </ul>
                <a href="/carreras/" class="mega-menu__ver-todas">Ver las 15 carreras disponibles →</a>
              </div>
            </div>
          </div>
        </li>
        <li class="header__item">
          <a href="/acuerdo-286/" class="header__link">Acuerdo 286</a>
        </li>
        <li class="header__item">
          <a href="/blog/" class="header__link">Blog</a>
        </li>
        <li class="header__item">
          <a href="/contacto/" class="header__link header__link--cta btn btn--primary">Evalúa tu Perfil Gratis</a>
        </li>
      </ul>
    </nav>

    <!-- Mobile Toggle -->
    <button class="mob-toggle" id="mob-toggle" aria-label="Abrir menú" aria-expanded="false" type="button">
      <svg class="mob-toggle__icon mob-toggle__open" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
      <svg class="mob-toggle__icon mob-toggle__close" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
    </button>
  </div>
</header>

<!-- ========== MOBILE FULLSCREEN MENU ========== -->
<div class="mob-menu" id="mob-menu" aria-hidden="true">
  <div class="mob-menu__inner">
    <nav class="mob-menu__nav">
      <a href="/titulacion-por-experiencia-laboral/" class="mob-menu__item">
        <span class="mob-menu__icon">📋</span>
        <span class="mob-menu__label">Titulación por Experiencia</span>
      </a>

      <button class="mob-menu__item mob-menu__item--expandable" id="mob-carreras-toggle" type="button">
        <span class="mob-menu__icon">🎓</span>
        <span class="mob-menu__label">Carreras</span>
        <svg class="mob-menu__chevron" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"/></svg>
      </button>
      <div class="mob-menu__submenu" id="mob-carreras-sub">
        <p class="mob-menu__sub-heading">🔒 Reguladas</p>
        {carrerasReguladas.map(c => (
          <a href={`/carreras/${c.slug}/`} class="mob-menu__sub-link">{c.nombre}</a>
        ))}
        <p class="mob-menu__sub-heading">✅ No Reguladas</p>
        {carrerasNoReguladas.map(c => (
          <a href={`/carreras/${c.slug}/`} class="mob-menu__sub-link">{c.nombre}</a>
        ))}
        <a href="/carreras/" class="mob-menu__sub-all">Ver las 15 carreras →</a>
      </div>

      <a href="/acuerdo-286/" class="mob-menu__item">
        <span class="mob-menu__icon">📜</span>
        <span class="mob-menu__label">Acuerdo 286</span>
      </a>
      <a href="/cedula-profesional/" class="mob-menu__item">
        <span class="mob-menu__icon">🏛️</span>
        <span class="mob-menu__label">Cédula Profesional</span>
      </a>
      <a href="/blog/" class="mob-menu__item">
        <span class="mob-menu__icon">📝</span>
        <span class="mob-menu__label">Blog</span>
      </a>
    </nav>

    <a href="/contacto/" class="mob-menu__cta">
      Evalúa tu Perfil Gratis
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" width="20" height="20"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
    </a>

    <div class="mob-menu__footer">
      <a href="https://wa.me/5215512345678" class="mob-menu__wa">
        💬 WhatsApp Directo
      </a>
    </div>
  </div>
</div>

<style is:global>
  /* ===== HEADER ===== */
  .header {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: rgba(255,255,255,0.97);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(0,0,0,0.06);
    height: 72px;
  }
  .header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 100%;
  }
  .header__logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: #0F2640;
    font-size: 1.125rem;
    flex-shrink: 0;
    z-index: 1002;
  }
  .header__logo-icon { font-size: 1.5rem; }
  .header__logo-text strong { font-weight: 800; }

  /* ===== DESKTOP NAV ===== */
  .header__nav { display: none; }
  .header__menu {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    list-style: none;
  }
  .header__link {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 0.5rem 1rem;
    color: #495057;
    text-decoration: none;
    font-size: 0.9375rem;
    font-weight: 500;
    border-radius: 8px;
    transition: color 0.15s, background 0.15s;
    background: none;
    border: none;
    cursor: pointer;
    font-family: inherit;
    white-space: nowrap;
  }
  .header__link:hover { color: #1B3A5C; background: #F8F9FA; }
  .header__link--cta { padding: 0.5rem 1.25rem; font-size: 0.875rem; }
  .header__chevron {
    font-size: 0.625rem;
    transition: transform 0.3s;
    display: inline-flex;
  }

  /* ===== MEGA MENU ===== */
  .mega-menu {
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background: #fff;
    box-shadow: 0 25px 60px rgba(0,0,0,0.12), 0 0 0 1px rgba(0,0,0,0.04);
    opacity: 0;
    visibility: hidden;
    transform: translateY(8px);
    transition: opacity 0.3s, transform 0.3s, visibility 0.3s;
    z-index: 999;
    border-radius: 0 0 16px 16px;
    overflow: hidden;
  }
  .header__item--mega:hover .mega-menu,
  .header__item--mega:focus-within .mega-menu {
    opacity: 1; visibility: visible; transform: translateY(0);
  }
  .header__item--mega:hover .header__chevron { transform: rotate(180deg); }
  .mega-menu__inner {
    display: grid;
    grid-template-columns: 1fr 1.5fr 1fr;
  }
  .mega-menu__col { padding: 1.75rem 2rem; }
  .mega-menu__col:not(:last-child) { border-right: 1px solid #F1F3F5; }
  .mega-menu__col--recursos { background: rgba(232,168,56,0.04); }
  .mega-menu__heading {
    font-size: 0.6875rem; font-weight: 800; text-transform: uppercase;
    letter-spacing: 0.1em; color: #1B3A5C; margin-bottom: 4px;
  }
  .mega-menu__subtext { font-size: 0.6875rem; color: #ADB5BD; margin-bottom: 1rem; }
  .mega-menu__list { list-style: none; display: flex; flex-direction: column; gap: 1px; }
  .mega-menu__link {
    display: block; padding: 8px 12px; color: #495057;
    text-decoration: none; font-size: 0.9375rem; border-radius: 8px;
    transition: background 0.15s, color 0.15s;
  }
  .mega-menu__link:hover { background: #F8F9FA; color: #0F2640; }
  .mega-menu__ver-todas {
    display: inline-flex; margin-top: 1.25rem; padding: 0.625rem 1rem;
    font-size: 0.8125rem; font-weight: 700; color: #fff;
    background: linear-gradient(135deg, #F0A500, #CC8800);
    text-decoration: none; border-radius: 8px;
  }

  /* ===== MOBILE TOGGLE ===== */
  .mob-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    color: #343A40;
    -webkit-tap-highlight-color: transparent;
    z-index: 1002;
    position: relative;
  }
  .mob-toggle__icon {
    width: 24px;
    height: 24px;
    position: absolute;
    transition: opacity 0.2s, transform 0.2s;
  }
  .mob-toggle__close {
    opacity: 0;
    transform: rotate(-90deg);
  }
  .mob-toggle.is-active .mob-toggle__open {
    opacity: 0;
    transform: rotate(90deg);
  }
  .mob-toggle.is-active .mob-toggle__close {
    opacity: 1;
    transform: rotate(0);
  }

  /* ===== MOBILE FULLSCREEN MENU ===== */
  .mob-menu {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    height: 100dvh;
    background: #fff;
    z-index: 999;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s;
    overflow: hidden;
  }
  .mob-menu.is-open {
    opacity: 1;
    visibility: visible;
  }
  .mob-menu__inner {
    display: flex;
    flex-direction: column;
    height: 100%;
    padding: 84px 20px 24px;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
  }
  .mob-menu__nav {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  /* Menu items */
  .mob-menu__item {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 16px 12px;
    color: #1a1a2e;
    text-decoration: none;
    font-size: 17px;
    font-weight: 500;
    border-bottom: 1px solid #f0f0f0;
    -webkit-tap-highlight-color: transparent;
    background: none;
    border-left: none;
    border-right: none;
    border-top: none;
    width: 100%;
    text-align: left;
    cursor: pointer;
    font-family: inherit;
    transition: background 0.15s;
  }
  .mob-menu__item:active { background: #f5f7fa; }
  .mob-menu__icon { font-size: 20px; flex-shrink: 0; width: 28px; text-align: center; }
  .mob-menu__label { flex: 1; }

  /* Expandable chevron */
  .mob-menu__chevron {
    width: 20px;
    height: 20px;
    color: #adb5bd;
    transition: transform 0.3s ease;
    flex-shrink: 0;
  }
  .mob-menu__item--expandable.is-open .mob-menu__chevron {
    transform: rotate(180deg);
  }

  /* Submenu */
  .mob-menu__submenu {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.35s ease;
    background: #f8f9fa;
    border-radius: 0 0 12px 12px;
    margin: 0 0 4px;
  }
  .mob-menu__submenu.is-open {
    max-height: 800px;
  }
  .mob-menu__sub-heading {
    font-size: 11px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #1B3A5C;
    padding: 14px 16px 6px;
    margin: 0;
  }
  .mob-menu__sub-link {
    display: block;
    padding: 10px 16px 10px 28px;
    color: #495057;
    text-decoration: none;
    font-size: 15px;
    transition: background 0.15s;
    -webkit-tap-highlight-color: transparent;
  }
  .mob-menu__sub-link:active { background: #e9ecef; }
  .mob-menu__sub-all {
    display: block;
    padding: 12px 16px;
    margin: 8px 12px 12px;
    color: #0a1932;
    font-weight: 700;
    font-size: 14px;
    text-align: center;
    text-decoration: none;
    background: linear-gradient(135deg, rgba(232,168,56,0.15), rgba(232,168,56,0.08));
    border-radius: 10px;
    -webkit-tap-highlight-color: transparent;
  }

  /* CTA button */
  .mob-menu__cta {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    margin: 16px 0 12px;
    padding: 18px;
    background: linear-gradient(135deg, #F0A500, #CC8800);
    color: #0a1932;
    font-weight: 700;
    font-size: 17px;
    text-align: center;
    text-decoration: none;
    border-radius: 14px;
    box-shadow: 0 4px 20px rgba(232,168,56,0.35);
    -webkit-tap-highlight-color: transparent;
    transition: transform 0.15s;
    flex-shrink: 0;
  }
  .mob-menu__cta:active { transform: scale(0.97); }

  /* Footer */
  .mob-menu__footer {
    padding: 12px 0 0;
    border-top: 1px solid #f0f0f0;
    flex-shrink: 0;
  }
  .mob-menu__wa {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 14px;
    color: #25d366;
    font-weight: 600;
    font-size: 15px;
    text-decoration: none;
    border-radius: 12px;
    background: rgba(37,211,102,0.08);
    -webkit-tap-highlight-color: transparent;
  }

  /* ===== DESKTOP ===== */
  @media (min-width: 1024px) {
    .header__nav { display: flex; }
    .mob-toggle { display: none; }
    .mob-menu { display: none !important; }
  }
</style>

<script is:inline>
(function(){
  var toggle = document.getElementById('mob-toggle');
  var menu = document.getElementById('mob-menu');
  var carrerasToggle = document.getElementById('mob-carreras-toggle');
  var carrerasSub = document.getElementById('mob-carreras-sub');

  if (!toggle || !menu) return;

  toggle.addEventListener('click', function(){
    var isOpen = menu.classList.contains('is-open');
    if (isOpen) {
      menu.classList.remove('is-open');
      menu.setAttribute('aria-hidden', 'true');
      toggle.classList.remove('is-active');
      toggle.setAttribute('aria-expanded', 'false');
      toggle.setAttribute('aria-label', 'Abrir menú');
      document.body.style.overflow = '';
    } else {
      menu.classList.add('is-open');
      menu.setAttribute('aria-hidden', 'false');
      toggle.classList.add('is-active');
      toggle.setAttribute('aria-expanded', 'true');
      toggle.setAttribute('aria-label', 'Cerrar menú');
      document.body.style.overflow = 'hidden';
    }
  });

  if (carrerasToggle && carrerasSub) {
    carrerasToggle.addEventListener('click', function(){
      var isOpen = carrerasSub.classList.contains('is-open');
      carrerasSub.classList.toggle('is-open');
      carrerasToggle.classList.toggle('is-open');
    });
  }

  // Close on link click
  var links = menu.querySelectorAll('a[href]');
  for (var i = 0; i < links.length; i++) {
    links[i].addEventListener('click', function(){
      menu.classList.remove('is-open');
      toggle.classList.remove('is-active');
      toggle.setAttribute('aria-expanded', 'false');
      document.body.style.overflow = '';
    });
  }

  // Header scroll shadow
  var header = document.getElementById('header');
  if (header) {
    var lastScroll = 0;
    window.addEventListener('scroll', function(){
      var y = window.scrollY;
      header.style.boxShadow = y > 10 ? '0 4px 20px rgba(0,0,0,0.06)' : 'none';
      lastScroll = y;
    }, { passive: true });
  }
})();
</script>
