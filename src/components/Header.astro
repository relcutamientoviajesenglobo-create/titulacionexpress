---
interface Props {
  currentPath?: string;
}
const { currentPath = '' } = Astro.props;

const carrerasReguladas = [
  { nombre: 'Derecho', slug: 'derecho' },
  { nombre: 'Contaduría', slug: 'contaduria' },
  { nombre: 'Ingeniería Industrial', slug: 'ingenieria-industrial' },
  { nombre: 'Ingeniería Computacional', slug: 'ingenieria-computacional' },
];

const carrerasNoReguladas = [
  { nombre: 'Administración', slug: 'administracion' },
  { nombre: 'Mercadotecnia', slug: 'mercadotecnia' },
  { nombre: 'Pedagogía', slug: 'pedagogia' },
  { nombre: 'Ciencias de la Comunicación', slug: 'ciencias-de-la-comunicacion' },
  { nombre: 'Ciencias Políticas', slug: 'ciencias-politicas' },
  { nombre: 'Turismo', slug: 'turismo' },
  { nombre: 'Gastronomía', slug: 'gastronomia' },
  { nombre: 'Diseño Gráfico', slug: 'diseno-grafico' },
  { nombre: 'Economía', slug: 'economia' },
  { nombre: 'Comercio Internacional', slug: 'comercio-internacional' },
  { nombre: 'Educación Preescolar', slug: 'educacion-preescolar' },
];

const recursos = [
  { nombre: 'Acuerdo 286 — Guía Completa', url: '/acuerdo-286/' },
  { nombre: 'Requisitos', url: '/titulacion-por-experiencia-laboral/requisitos/' },
  { nombre: 'Proceso Paso a Paso', url: '/titulacion-por-experiencia-laboral/proceso-paso-a-paso/' },
  { nombre: 'Costos', url: '/titulacion-por-experiencia-laboral/costo/' },
  { nombre: 'Cédula Profesional', url: '/cedula-profesional/' },
  { nombre: 'Preguntas Frecuentes', url: '/titulacion-por-experiencia-laboral/preguntas-frecuentes/' },
];
---

<header class="header" id="header">
  <div class="header__inner container">
    <a href="/" class="header__logo" aria-label="Titulación Express — Inicio">
      <span class="header__logo-icon">🎓</span>
      <span class="header__logo-text"><strong>Titulación</strong> Express</span>
    </a>

    <!-- Desktop Navigation -->
    <nav class="header__nav" aria-label="Navegación principal">
      <ul class="header__menu">
        <li class="header__item">
          <a href="/titulacion-por-experiencia-laboral/" class="header__link">Titulación</a>
        </li>
        <li class="header__item header__item--mega">
          <button class="header__link header__link--dropdown" aria-expanded="false" aria-haspopup="true">
            Carreras <span class="header__chevron" aria-hidden="true">▾</span>
          </button>
          <div class="mega-menu" role="menu" aria-label="Carreras disponibles">
            <div class="mega-menu__inner container">
              <div class="mega-menu__col">
                <h3 class="mega-menu__heading">Carreras Reguladas</h3>
                <p class="mega-menu__subtext">Requieren 50% de créditos universitarios</p>
                <ul class="mega-menu__list">
                  {carrerasReguladas.map(c => (
                    <li><a href={`/carreras/${c.slug}/`} class="mega-menu__link">{c.nombre}</a></li>
                  ))}
                </ul>
              </div>
              <div class="mega-menu__col">
                <h3 class="mega-menu__heading">Carreras No Reguladas</h3>
                <p class="mega-menu__subtext">Sin requisito de créditos previos</p>
                <ul class="mega-menu__list">
                  {carrerasNoReguladas.map(c => (
                    <li><a href={`/carreras/${c.slug}/`} class="mega-menu__link">{c.nombre}</a></li>
                  ))}
                </ul>
              </div>
              <div class="mega-menu__col mega-menu__col--recursos">
                <h3 class="mega-menu__heading">Recursos</h3>
                <ul class="mega-menu__list">
                  {recursos.map(r => (
                    <li><a href={r.url} class="mega-menu__link">{r.nombre}</a></li>
                  ))}
                </ul>
                <a href="/carreras/" class="mega-menu__ver-todas">Ver las 15 carreras disponibles →</a>
              </div>
            </div>
          </div>
        </li>
        <li class="header__item">
          <a href="/acuerdo-286/" class="header__link">Acuerdo 286</a>
        </li>
        <li class="header__item">
          <a href="/blog/" class="header__link">Blog</a>
        </li>
        <li class="header__item">
          <a href="/contacto/" class="header__link header__link--cta btn btn--primary">Evalúa tu Perfil Gratis</a>
        </li>
      </ul>
    </nav>

    <!-- Mobile Toggle -->
    <button class="header__mobile-toggle" aria-label="Menú" aria-expanded="false" id="mobile-toggle" type="button">
      <span class="header__hamburger"></span>
    </button>
  </div>

  <!-- Mobile Full Screen Menu -->
  <div class="mobile-nav" id="mobile-nav">
    <a href="/titulacion-por-experiencia-laboral/" class="mobile-nav__link">Titulación por Experiencia</a>
    <div class="mobile-nav__accordion">
      <button class="mobile-nav__link mobile-nav__accordion-btn" type="button" id="mob-carreras-btn">
        Carreras
        <span class="mobile-nav__chevron">›</span>
      </button>
      <div class="mobile-nav__panel" id="mob-carreras-panel">
        <p class="mobile-nav__sub-heading">Reguladas</p>
        {carrerasReguladas.map(c => (
          <a href={`/carreras/${c.slug}/`} class="mobile-nav__sub-link">{c.nombre}</a>
        ))}
        <p class="mobile-nav__sub-heading">No Reguladas</p>
        {carrerasNoReguladas.map(c => (
          <a href={`/carreras/${c.slug}/`} class="mobile-nav__sub-link">{c.nombre}</a>
        ))}
        <a href="/carreras/" class="mobile-nav__sub-link mobile-nav__sub-link--all">Ver todas las carreras →</a>
      </div>
    </div>
    <a href="/acuerdo-286/" class="mobile-nav__link">Acuerdo 286</a>
    <a href="/cedula-profesional/" class="mobile-nav__link">Cédula Profesional</a>
    <a href="/blog/" class="mobile-nav__link">Blog</a>
    <a href="/contacto/" class="mobile-nav__cta">Evalúa tu Perfil Gratis →</a>
  </div>
</header>

<!-- ALL STYLES GLOBAL — no Astro scoping issues -->
<style is:global>
  /* ===== HEADER ===== */
  .header {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: rgba(255,255,255,0.95);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(0,0,0,0.06);
    height: 72px;
  }
  .header__inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 100%;
  }
  .header__logo {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    text-decoration: none;
    color: #0F2640;
    font-size: 1.125rem;
    flex-shrink: 0;
  }
  .header__logo-icon { font-size: 1.5rem; }
  .header__logo-text strong { font-weight: 800; }

  /* ===== DESKTOP NAV ===== */
  .header__nav { display: none; }
  .header__menu {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    list-style: none;
  }
  .header__link {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 0.5rem 1rem;
    color: #495057;
    text-decoration: none;
    font-size: 0.9375rem;
    font-weight: 500;
    border-radius: 8px;
    transition: color 0.15s, background 0.15s;
    background: none;
    border: none;
    cursor: pointer;
    font-family: inherit;
    white-space: nowrap;
  }
  .header__link:hover { color: #1B3A5C; background: #F8F9FA; }
  .header__link--cta { padding: 0.5rem 1.25rem; font-size: 0.875rem; }
  .header__chevron {
    font-size: 0.625rem;
    transition: transform 0.3s;
    display: inline-flex;
  }

  /* ===== MEGA MENU ===== */
  .mega-menu {
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    background: #fff;
    box-shadow: 0 25px 60px rgba(0,0,0,0.12), 0 0 0 1px rgba(0,0,0,0.04);
    opacity: 0;
    visibility: hidden;
    transform: translateY(8px);
    transition: opacity 0.3s, transform 0.3s, visibility 0.3s;
    z-index: 999;
    border-radius: 0 0 16px 16px;
    overflow: hidden;
  }
  .header__item--mega:hover .mega-menu,
  .header__item--mega:focus-within .mega-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }
  .header__item--mega:hover .header__chevron { transform: rotate(180deg); }
  .mega-menu__inner {
    display: grid;
    grid-template-columns: 1fr 1.5fr 1fr;
  }
  .mega-menu__col { padding: 1.75rem 2rem; }
  .mega-menu__col:not(:last-child) { border-right: 1px solid #F1F3F5; }
  .mega-menu__col--recursos { background: rgba(232,168,56,0.04); }
  .mega-menu__heading {
    font-size: 0.6875rem;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #1B3A5C;
    margin-bottom: 4px;
  }
  .mega-menu__subtext {
    font-size: 0.6875rem;
    color: #ADB5BD;
    margin-bottom: 1rem;
  }
  .mega-menu__list { list-style: none; display: flex; flex-direction: column; gap: 1px; }
  .mega-menu__link {
    display: block;
    padding: 8px 12px;
    color: #495057;
    text-decoration: none;
    font-size: 0.9375rem;
    border-radius: 8px;
    transition: background 0.15s, color 0.15s;
  }
  .mega-menu__link:hover { background: #F8F9FA; color: #0F2640; }
  .mega-menu__ver-todas {
    display: inline-flex;
    margin-top: 1.25rem;
    padding: 0.625rem 1rem;
    font-size: 0.8125rem;
    font-weight: 700;
    color: #fff;
    background: linear-gradient(135deg, #E8A838, #C78A1E);
    text-decoration: none;
    border-radius: 8px;
  }

  /* ===== MOBILE TOGGLE ===== */
  .header__mobile-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0;
    -webkit-tap-highlight-color: transparent;
    z-index: 1002;
  }
  .header__hamburger,
  .header__hamburger::before,
  .header__hamburger::after {
    display: block;
    width: 22px;
    height: 2px;
    background: #343A40;
    border-radius: 2px;
    transition: all 0.3s ease;
  }
  .header__hamburger { position: relative; }
  .header__hamburger::before,
  .header__hamburger::after {
    content: '';
    position: absolute;
    left: 0;
  }
  .header__hamburger::before { top: -7px; }
  .header__hamburger::after { top: 7px; }

  /* X state */
  .header__mobile-toggle.is-active .header__hamburger { background: transparent; }
  .header__mobile-toggle.is-active .header__hamburger::before { transform: rotate(45deg); top: 0; }
  .header__mobile-toggle.is-active .header__hamburger::after { transform: rotate(-45deg); top: 0; }

  /* ===== MOBILE NAV — FULL SCREEN ===== */
  .mobile-nav {
    display: none;
    position: fixed;
    top: 72px;
    left: 0;
    right: 0;
    bottom: 0;
    background: #fff;
    z-index: 999;
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
    padding: 0 0 100px;
    flex-direction: column;
  }
  .mobile-nav.is-open {
    display: flex;
  }

  .mobile-nav__link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 16px 20px;
    color: #1a1a2e;
    text-decoration: none;
    font-size: 17px;
    font-weight: 500;
    border-bottom: 1px solid #f0f0f0;
    -webkit-tap-highlight-color: transparent;
    background: none;
    border-left: none;
    border-right: none;
    border-top: none;
    width: 100%;
    text-align: left;
    cursor: pointer;
    font-family: inherit;
  }
  .mobile-nav__link:active { background: #f5f5f5; }

  .mobile-nav__chevron {
    font-size: 20px;
    font-weight: 300;
    color: #aaa;
    transition: transform 0.2s;
  }
  .mobile-nav__accordion-btn.is-open .mobile-nav__chevron {
    transform: rotate(90deg);
  }

  .mobile-nav__panel {
    display: none;
    padding: 0 12px 12px 12px;
    background: #f9f9f9;
  }
  .mobile-nav__panel.is-open { display: block; }

  .mobile-nav__sub-heading {
    font-size: 11px;
    font-weight: 800;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: #1B3A5C;
    padding: 12px 12px 4px;
    margin: 0;
  }
  .mobile-nav__sub-link {
    display: block;
    padding: 8px 12px;
    color: #6C757D;
    text-decoration: none;
    font-size: 15px;
    border-radius: 8px;
    -webkit-tap-highlight-color: transparent;
  }
  .mobile-nav__sub-link:active { background: #e9ecef; color: #1B3A5C; }
  .mobile-nav__sub-link--all {
    color: #C78A1E;
    font-weight: 700;
    margin-top: 8px;
    padding: 10px 12px;
    background: rgba(232,168,56,0.08);
    border-radius: 8px;
  }

  .mobile-nav__cta {
    display: block;
    margin: 20px 16px 0;
    padding: 16px;
    background: linear-gradient(135deg, #E8A838, #C78A1E);
    color: #0a1932;
    font-weight: 700;
    font-size: 17px;
    text-align: center;
    text-decoration: none;
    border-radius: 12px;
    -webkit-tap-highlight-color: transparent;
  }
  .mobile-nav__cta:active { opacity: 0.9; }

  /* ===== DESKTOP BREAKPOINT ===== */
  @media (min-width: 1024px) {
    .header__nav { display: flex; }
    .header__mobile-toggle { display: none; }
    .mobile-nav { display: none !important; }
  }
</style>

<script is:inline>
(function(){
  var toggle = document.getElementById('mobile-toggle');
  var nav = document.getElementById('mobile-nav');
  var carrerasBtn = document.getElementById('mob-carreras-btn');
  var carrerasPanel = document.getElementById('mob-carreras-panel');

  if (toggle && nav) {
    toggle.addEventListener('click', function(){
      var isOpen = nav.classList.contains('is-open');
      if (isOpen) {
        nav.classList.remove('is-open');
        toggle.classList.remove('is-active');
        toggle.setAttribute('aria-expanded', 'false');
        document.body.style.overflow = '';
      } else {
        nav.classList.add('is-open');
        toggle.classList.add('is-active');
        toggle.setAttribute('aria-expanded', 'true');
        document.body.style.overflow = 'hidden';
      }
    });
  }

  if (carrerasBtn && carrerasPanel) {
    carrerasBtn.addEventListener('click', function(){
      var isOpen = carrerasPanel.classList.contains('is-open');
      if (isOpen) {
        carrerasPanel.classList.remove('is-open');
        carrerasBtn.classList.remove('is-open');
      } else {
        carrerasPanel.classList.add('is-open');
        carrerasBtn.classList.add('is-open');
      }
    });
  }

  // Header scroll shadow
  var header = document.getElementById('header');
  if (header) {
    window.addEventListener('scroll', function(){
      if (window.scrollY > 10) {
        header.style.boxShadow = '0 4px 20px rgba(0,0,0,0.06)';
      } else {
        header.style.boxShadow = 'none';
      }
    });
  }
})();
</script>
